# Branch `main` — JdbcTemplate

This branch implements the persistence layer using Spring `JdbcTemplate` + RowMappers.

## Configuration
DB properties are read from `src/main/resources/database.properties`:
- `driver`
- `url`
- `user`
- `password`
